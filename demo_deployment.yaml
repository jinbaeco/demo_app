apiVersion: apps/v1
kind: Deployment
metadata:
  name: demoapp_deploy
spec:
  replicas: 2
  selector:
    matchLabels:
      type: pipeline
      version: v1
  template:
    metadata:
      labels:
        type: pipeline
        version: v1
    spec:
      containers:
        - name: demo_app
          image: jinbaeco/demo_app:latest
          ports:
            - containerPort: 8080  